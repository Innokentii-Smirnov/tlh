query EnglishTranslation($form: String!, $pos: String!, $deu: String!) {
  stem(form: $form, pos: $pos, deu: $deu) {
    eng
  }
}

query AllStems {
  allStems {
    form
    pos
    deu
    eng
  }
}
